import 'package:flutter/material.dart';

class SampleLessonScreen extends StatelessWidget {
  const SampleLessonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      
    );
  }
}